<?php
/**
 * Learn section gate.
 *
 * Previously this redirected the whole Learn section to /jobs/ while the academy
 * was being built. The section is now live, so this is a no-op. The file is kept
 * (it's required across learn/*) so enabling/disabling is a one-line change.
 */
// To disable the section again, uncomment:
// header('Location: /jobmington/jobs/', true, 302); exit;
